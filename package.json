{
	"scripts": {
		"postinstall": "cd client && npm i",
		"//dev": "lerna run dev",
		"//build": "lerna run build",
		"dev": "BASE_URL=http://localhost:8081 ./node_modules/.bin/parcel client/index.html -p 8080  --open & php -S localhost:8081 server/api.php -t server",
		"build": "BASE_URL=./api.php ./node_modules/.bin/parcel build client/index.html --public-url ./ --out-dir dist && cp -R server/* dist",
		"serve": "php -S localhost:8080 -t dist"
	},
	"devDependencies": {
		"autoprefixer": "^8.4.1",
		"babel-plugin-transform-class-properties": "^6.24.1",
		"babel-preset-env": "^1.6.1",
		"parcel-bundler": "^1.7.1",
		"postcss-modules": "^1.1.0"
	}
}
